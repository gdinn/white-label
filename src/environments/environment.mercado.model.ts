export interface EnvironmentMercadoModel {
  nome: string, 
  baseref: string,
  cores: {
    primary: string,
    secondary: string,
    tertiary: string,
    quarternary: string,
    pentenary: string
  }
}